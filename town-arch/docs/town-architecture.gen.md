# Town architecture

_Generated by `mix arch_lens.town.map` — do not edit by hand._

## Apps

- **Claude Code Course** (`claude_code_course`, schema v3) — contexts 22, actors 6, resources 26, oban_workers 0, entry_points 58, external_systems 5 · aliases: `ccc`, `claude-code-course`, `claude.handgemacht.ai`
- **Eval Lab** (`eval_lab`, schema v3) — contexts 14, actors 3, resources 20, oban_workers 5, entry_points 80, external_systems 3 · aliases: `eval_lab`, `eval-lab`
- **HAVI** (`havi`, schema v3) — contexts 15, actors 5, resources 14, oban_workers 2, entry_points 90, external_systems 5 · aliases: `havi`, `havi.handgemacht.ai`

## Cross-app links

- **claude_code_course** → **havi** — `http` (declared_external) · provenance: collected+declared · matched on `havi` · endpoint `GET /api/annotations` → `HaviWeb.AnnotationController` (api) · _annotation read-back API the facts-review admin pages query for open-comment counts_

## Unresolved externals

### claude_code_course

- `external:brevo` (http_boundary https://api.brevo.com)
- `external:claude` (manual spawned as a `claude -p` subprocess via System.cmd in ClaudeCodeCourse.ClaudeCLI; no HTTP boundary or dependency vendor for the collector to detect)
- `external:jina` (dep ash_knowledge_graph)
- `external:stripe` (dep stripity_stripe)

### eval_lab

- `external:docker` (module EvalLab.Ports.Docker)
- `external:dolt` (module EvalLab.Ports.Dolt)
- `external:git` (module EvalLab.Ports.Git)

### havi

- `external:brevo` (swoosh_adapter Swoosh.Adapters.Brevo)
- `external:cloudflare` (http_boundary https://api.cloudflare.com)
- `external:opentelemetry` — OpenTelemetry (dep opentelemetry, dep opentelemetry_api, dep opentelemetry_bandit, dep opentelemetry_ecto, dep opentelemetry_exporter, dep opentelemetry_phoenix, dep opentelemetry_semantic_conventions)
- `external:sentry` (dep sentry)
- `external:stripe` (dep stripity_stripe, http_boundary https://api.stripe.com)
